name: Web to APK Converter

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Kodları Çek
      uses: actions/checkout@v4

    - name: 🟢 Node.js Kurulumu
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 📦 Web Paketlerini Yükle
      run: npm install

    - name: 🏗️ Web Sitesini İnşa Et (Build)
      run: npm run build

    - name: 📱 Capacitor Kurulumu
      run: |
        npm install @capacitor/core @capacitor/cli @capacitor/android
        npx cap init "Uygulamam" "com.salih.uygulama" --web-dir dist
        npx cap add android

    - name: ☕ Java Kurulumu (GÜNCELLENDİ: v21)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21' # Hata burada çözüldü (17 -> 21 yapıldı)

    - name: 🛠️ APK İnşa Et
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug

    - name: 🎁 APK'yı Teslim Et
      uses: actions/upload-artifact@v4
      with:
        name: web-donusturulmus-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
