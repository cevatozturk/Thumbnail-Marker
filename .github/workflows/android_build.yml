name: Android Diagnostic

on: [push]

jobs:
  check-files:
    runs-on: ubuntu-latest
    steps:
    - name: 📥 Kodları Çek
      uses: actions/checkout@v4

    - name: 📂 Dosya Yapısını Listele (Teşhis)
      run: |
        echo "--- ANA DİZİN LİSTESİ ---"
        ls -la
        echo "--- GRADLEW ARANIYOR ---"
        find . -name "gradlew"
